<policies>
    <inbound>
        <base />
        <!-- Validate JWT token from Entra ID -->
        <validate-jwt header-name="Authorization" failed-validation-httpcode="401" failed-validation-error-message="Unauthorized. Access token is missing or invalid.">
            <openid-config url="https://login.microsoftonline.com/{entra-internal-tenant-id}/v2.0/.well-known/openid-configuration" />
            <required-claims>
                <claim name="aud" match="any">
                    <value>{entra-internal-client-id}</value>
                </claim>
            </required-claims>
        </validate-jwt>
        <!-- IP whitelisting for admin endpoints -->
        <choose>
            <when condition="@(context.Request.Url.Path.Contains("/admin"))">
                <ip-filter action="allow">
                    <address-range from="10.0.0.0" to="10.255.255.255" />
                </ip-filter>
            </when>
        </choose>
        <!-- Rate limiting (higher for internal) -->
        <rate-limit-by-key calls="5000" renewal-period="60" counter-key="@(context.Request.IpAddress)" />
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>

